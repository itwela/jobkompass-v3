import { v } from "convex/values";
import { defineSchema } from "convex/server";

export default {
  providers: [
    {
      domain: "https://proficient-mammoth-632.convex.cloud",
      applicationID: "convex",
    }
  ]
};
